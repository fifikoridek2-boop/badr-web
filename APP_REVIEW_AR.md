# تقرير فحص تطبيق بدر (تحديث بعد إعادة المحاولة بالإنترنت)

## 1) ملخص ما يظهر في التطبيق (من الكود)
- تطبيق Flutter باسم **بدر** باتجاه RTL وخط Cairo، يبدأ بـ Splash ثم ينتقل إلى واجهة رئيسية.  
- شريط تنقل سفلي يحتوي: **الرئيسية، المصحف، الأذكار، المكتبة، الإعدادات**.  
- يوجد **مشغّل صوت** (مصغّر/موسّع) يظهر أثناء تشغيل التلاوة أو الراديو.

## 2) سلوك الواجهات
- **Splash**: شعار + اسم التطبيق + نص "القرآن الكريم والأذكار" + مؤشر تحميل، ثم انتقال تلقائي.  
- **الرئيسية**: مواقيت صلاة + ذكر عشوائي + اختصارات (مصحف/مكتبة/تسبيح/راديو) مع تحديث بالسحب.  
- **المصحف**: قائمة سور ثم شاشة تفاصيل السورة.  
- **الأذكار**: أقسام أذكار مع نسخ النص.  
- **المكتبة/الراديو**: تشغيل تلاوات/بث مباشر مع تحكمات التشغيل.  
- **التسبيح**: عدّاد مع إجمالي.

## 3) إعادة المحاولة بالإنترنت (حسب طلبك)
تمت إعادة المحاولة فعليًا لإخراج معاينة ويب من هذه البيئة، وكانت النتائج:
- `flutter --version` → فشل لأن Flutter غير مثبّت في البيئة.
- محاولة تنزيل Flutter SDK من Google Storage → **403 Forbidden**.
- اختبار الوصول إلى `github.com`, `raw.githubusercontent.com`, `storage.googleapis.com` → جميعها **403 Forbidden** من شبكة البيئة الحالية.

**النتيجة:** لا يمكن من داخل هذه الجلسة تثبيت Flutter أو بناء Web أو نشر رابط حي خارجي، بسبب حظر شبكة البنية نفسها (وليس من إعدادات مشروعك).

## 4) ملاحظة كود مهمة قبل النشر
- في `lib/main.dart` يوجد تكرار لنفس الـ Providers (`QuranProvider`, `AzkarProvider`, `LibraryProvider`, `PrayerProvider`) مرتين داخل `MultiProvider`.
- يفضّل إزالة التكرار قبل أي إطلاق إنتاجي لتفادي سلوك حالة غير متوقع.

## 5) نشر ويب آمن بدون التأثير على APK (موصى به)
لضمان عدم الإضرار بمسار APK:
1. لا تعدّل `android/` ولا إعدادات التوقيع.
2. ابنِ الويب فقط: `flutter build web --release`
3. ارفع `build/web` فقط إلى استضافة Static (Netlify / Firebase Hosting / Vercel).
4. أي إعدادات نشر تكون خارج مسار Android (ملفات CI/hosting فقط).

## 6) أوامر جاهزة للتنفيذ على جهازك (خارج هذه البيئة المحجوبة)
```bash
flutter pub get
flutter build web --release

# خيار سريع للمعاينة المحلية
cd build/web
python3 -m http.server 8080
# افتح: http://localhost:8080
```

### نشر Netlify Drop (بدون تعديل المشروع)
- افتح https://app.netlify.com/drop
- اسحب مجلد `build/web` وسيتم توليد رابط معاينة مباشر.

---

إذا أردت، في خطوة تالية أطبّق لك إصلاح تكرار الـ Providers فقط (تعديل آمن لا يمس APK) ثم أعطيك commit منفصل واضح.
